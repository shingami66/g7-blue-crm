services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - env_local
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
    env_file:
      - .env.local

secrets:
  env_local:
    file: .env.local
